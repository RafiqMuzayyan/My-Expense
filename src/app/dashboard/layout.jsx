export default function DashboardLayout({ children }) {
    return(
        <section className="">

            {/* sidebar */}

            {/* content */}
            <div>
                {children}
            </div>

        </section>
    )
}